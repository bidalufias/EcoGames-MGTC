{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && npm run build || { echo 'Pre-push build failed — fix before pushing' >&2; exit 2; }",
            "timeout": 240,
            "statusMessage": "Running pre-push build check (tsc + vite build)..."
          }
        ]
      }
    ]
  }
}
